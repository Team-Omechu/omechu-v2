// Supabase `Database` 타입 placeholder.
// DB가 live된 후 다음 명령으로 덮어쓴다:
//   supabase gen types typescript --project-id xztldvunnasjaxnzqpct --schema public > src/shared/lib/supabase/types.gen.ts
// 그리고 `Database`를 types.gen.ts에서 re-export 하도록 수정.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
